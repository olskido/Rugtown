/**
 * NoopSettlementAdapter.ts — re-exports the preserved Phase 10G Noop adapter.
 * The original implementation stays in ../SettlementAdapter.ts unchanged.
 */

export { NoopSettlementAdapter } from '../SettlementAdapter';
